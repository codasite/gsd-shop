<section class="page-content" data-section-id="{{ section.id }}">
  <div class="section-wrapper">
    <h1 class="page-title">{{ page.title }}</h1>
    <div class="page-body rte">
      {{ page.content }}
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Page Content",
  "settings": []
}
{% endschema %}
